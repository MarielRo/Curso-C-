#include <iostream>
#include <string>

using namespace std;
/*En las siguientes funciones se valida cada una de las fichas por aparte
 Para que no se coloquen en posiciciones que no deben o fichas que no son.
*/

//string ValidarCruz(int move);
//string ValidarEquis(int move);
//string ValidarCerrado(int move);
//string ValidarAbierto(int move);